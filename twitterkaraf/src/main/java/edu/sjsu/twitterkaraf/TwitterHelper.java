package edu.sjsu.twitterkaraf;

import java.net.URL;

import javax.net.ssl.HttpsURLConnection;

public class TwitterHelper {
	/*private final String TWITTER_HOST = "api.twitter.com";
	private final String TWITTER_AUTH_URL = "https://api.twitter.com/oauth2/token";
	private final String TWITTER_URL = "https://api.twitter.com/1.1/statuses/user_timeline.json?screen_name=INFAsupport&count=200";

	private HttpsURLConnection getHTTPSConnection(String method, String endpointUrl) {
		HttpsURLConnection connection = null;
		URL url = new URL(endpointUrl);
		connection = (HttpsURLConnection) url.openConnection();
		connection.setDoOutput(true);
		connection.setDoInput(true);
		connection.setRequestMethod(method);
		connection.setRequestProperty("Host", TWITTER_HOST);
		connection.setRequestProperty("User-Agent", TWITTER_HANDLE);
		connection.setUseCaches(false);
		return connection;
	}
*/
}
